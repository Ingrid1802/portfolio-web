<style>
    .floating-icon {
      animation: float 6s ease-in-out infinite;
    }
  
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0px); }
    }
  
    .circle-ring {
      animation: rotate 20s linear infinite;
    }
  
    @keyframes rotate {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
  </style>
  
  <section id="home" class="mt-[5rem] px-8">
    <div class="max-w-6xl w-full grid grid-cols-1 md:grid-cols-5 gap-8 items-start">
      <!-- Texto -->
      <div class="space-y-2 md:col-span-3 ml-[5rem]">
        <p class="text-purple-500 text-sm mb-[1rem]"><b>Hi, my name is</b></p>
        <h1 class="text-4xl sm:text-5xl font-bold mb-[1rem]">Ingrid Karen Aybar Camacho</h1>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-700 mb-[1rem]">
          <span class="typing-text">If you can imagine it, you can code it.</span>
        </h2>
        <p class="text-gray-700 max-w-md text-sm mb-[1rem]">
            I'm a Computer Systems Engineering student and a graduate of the Laboratoria bootcamp. I'm taking my first steps as a web developer, strengthening my technical and creative skills to kickstart my career in the tech world.
        </p>
      </div>
  
      <!-- Imagen con iconos flotando -->
      <div class="relative w-full h-[350px] md:h-[360px] flex justify-center items-center md:col-span-2 bg-[#5c5470] rounded-2xl shadow-lg">
        <div class="relative w-60 h-60 rounded-full border-4 border-[#7b2cbf] shadow-md z-10 overflow-hidden">
          <img src="/foto.jpeg" alt="Tu foto" class="w-full h-full object-cover rounded-full" />
        </div>
  
        <!-- Anillo giratorio más grande -->
        <div class="absolute w-80 h-80 border border-blue-300 rounded-full circle-ring z-0"></div>
  
        <!-- Íconos tech alrededor del círculo -->
        <div class="absolute top-[10%] left-[50%] transform -translate-x-1/2 floating-icon">
          <img src="/icons/html.png" class="w-8 h-8" loading="lazy" />
        </div>
        <div class="absolute top-[30%] right-[15%] floating-icon">
          <img src="/icons/css.png" class="w-8 h-8" loading="lazy" />
        </div>
        <div class="absolute bottom-[25%] right-[10%] floating-icon">
          <img src="/icons/react.png" class="w-8 h-8" loading="lazy" />
        </div>
        <div class="absolute bottom-[15%] left-[15%] floating-icon">
          <img src="/icons/js.png" class="w-8 h-8" loading="lazy" />
        </div>
        <div class="absolute top-[30%] left-[10%] floating-icon">
          <img src="/icons/bootstrap.png" class="w-10 h-10" loading="lazy" />
        </div>
        <div class="absolute bottom-[5%] left-[48%] transform -translate-x-1/2 floating-icon">
          <img src="/icons/astro.png" class="w-8 h-8" loading="lazy" />
        </div>
      </div>
    </div>
  </section>
  